{% extends 'base.html.twig' %}

{% block title %}Equipment Management - {{ parent() }}{% endblock %}

{% block body %}
<div class="animate-fade-in">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>Equipment Management</h1>
        <a href="{{ path('app_admin_equipment_new') }}" class="btn btn-primary">
            <i class="fas fa-plus-circle me-1"></i> Add Equipment
        </a>
    </div>

    <div class="card">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Description</th>
                            <th>Icon</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for equipment in equipment %}
                            <tr>
                                <td>{{ equipment.name }}</td>
                                <td>{{ equipment.description }}</td>
                                <td><i class="fas {{ equipment.icon }}"></i></td>
                                <td>
                                    <div class="btn-group">
                                        <a href="{{ path('app_admin_equipment_edit', {'id': equipment.id}) }}" class="btn btn-sm btn-outline-primary">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                        <a href="{{ path('app_admin_equipment_delete', {'id': equipment.id}) }}" 
                                           class="btn btn-sm btn-outline-danger"
                                           onclick="return confirm('Are you sure you want to delete this equipment?')">
                                            <i class="fas fa-trash"></i>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="4" class="text-center">No equipment found</td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
{% endblock %}